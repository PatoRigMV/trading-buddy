# ladder_policy.yaml
# Mid±ticks ladder & retry policy per strategy bucket.

default:
  ticks: [-2, -1, 0, 1, 2, 3, 4]   # ladder around net mid (units are ticks)
  maxRetries: 7
  slippageBpsCap: 50               # 0.50% vs initial net mid

breakout_debit_spread:
  ticks: [-1, 0, 1, 2, 3, 4, 5]
  maxRetries: 6
  slippageBpsCap: 60

income_credit_spread:
  ticks: [0, 1, 2, 3, 4, 5, 6]
  maxRetries: 8
  slippageBpsCap: 40

iron_condor:
  ticks: [0, 1, 2, 3, 4, 5, 6]
  maxRetries: 10
  slippageBpsCap: 35

strangle:
  ticks: [0, 1, 2, 3, 4, 5]
  maxRetries: 6
  slippageBpsCap: 40
